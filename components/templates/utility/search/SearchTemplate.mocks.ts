import { SearchTemplateProps } from './SearchTemplate';

const Search: SearchTemplateProps = {
  sampleTextProp: 'Search Google or type a URL',
};

export const mockSearchTemplateProps = {
  Search,
};
